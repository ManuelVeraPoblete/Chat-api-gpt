model User {
  id               String   @id @default(uuid())
  email            String   @unique
  displayName      String
  passwordHash     String

  phone            String?
  companySection   String?
  jobTitle         String?

  refreshTokenHash String?
  createdAt        DateTime @default(now())
}
